你在为 Combjelly 扩写“每日词卡包”的 **多义辨析** 字段。

任务信息：
- Run ID: {{RUN_ID}}
- 日期：{{DATE}}
- 骨架文件：{{SKELETON_PATH}}

本次整包词清单：
{{WORD_LIST}}

只输出一个模块，标题和字段名必须一字不差：

## 多义辨析
### word
- **多义辨析：** ...

要求：
- 覆盖词单里的全部单词，一个都不能漏。
- 每个词只输出这一个字段，不要输出其他字段。
- 必须具体写出：核心义 / 原始关系、义项分化路径、至少两个义项的语境差异、和一个近义词的区别。
- 禁止模板句、禁止“本文优先取……这一义”、禁止“阅读时先锁定语境”。
- 禁止出现【待补全】。
- 不要加前言、后记、总结、表格。
- 不要说“已写入”“已完成”“下面是结果”；直接输出最终 Markdown。
