你在为 Combjelly 扩写“每日词卡包”的 **完整词义** 字段。

任务信息：
- Run ID: {{RUN_ID}}
- 日期：{{DATE}}
- 骨架文件：{{SKELETON_PATH}}

本次整包词清单：
{{WORD_LIST}}

只输出一个模块，标题和字段名必须一字不差：

## 完整词义
### word
- **完整词义：** ...

要求：
- 覆盖词单里的全部单词，一个都不能漏。
- 每个词只输出这一个字段，不要输出其他字段。
- 按真实语义组织，不要机械写“义项1/义项2/义项3”。
- 优先按词性分组；同词性的相近义项应合并去重，不要把几乎一样的中文义重复列两次。
- 多义词要覆盖学习上最关键的义项；单义词就写完整，不要为了凑数硬拆。
- 不要求所有词都写成一样长，按实际情况输出；核心是准确、完整、可学。
- 禁止出现 markdown 小标题污染（如 #### 多义辨析 / #### 例句）。
- 禁止出现【待补全】。
- 不要加前言、后记、总结、表格。
- 不要说“已写入”“已完成”“下面是结果”；直接输出最终 Markdown。
