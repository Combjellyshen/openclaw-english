你在为 Combjelly 扩写“每日词卡包”的 **单词派生** 字段。

任务信息：
- Run ID: {{RUN_ID}}
- 日期：{{DATE}}
- 骨架文件：{{SKELETON_PATH}}

本次整包词清单：
{{WORD_LIST}}

只输出一个模块，标题和字段名必须一字不差：

## 单词派生
### word
- **单词派生：** ...

要求：
- 覆盖词单里的全部单词，一个都不能漏。
- 每个词只输出这一个字段，不要输出其他字段。
- 要讲清原词和 2–4 个最有迁移价值的派生词 / 词族成员之间的关系，不要只机械列词。
- 如果某个词派生链弱，也要给出具体可学的词族方向；不要用空话兜底。
- 禁止出现【待补全】。
- 不要加前言、后记、总结、表格。
- 不要说“已写入”“已完成”“下面是结果”；直接输出最终 Markdown。
